# Klucze API z AMO: https://addons.mozilla.org/developers/addon/api/key/
# Skopiuj ten plik do .env i uzupełnij wartościami.
# .env jest ignorowany przez git — nie trafi do repozytorium.

AMO_JWT_ISSUER="user:..."
AMO_JWT_SECRET="..."
